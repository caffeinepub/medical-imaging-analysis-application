{
  "frontend/src/App.tsx": "Main application component that handles authentication flow, profile setup, and renders the dashboard with theme provider and toast notifications, with proper type handling for userProfile prop.",
  "frontend/src/hooks/useQueries.ts": "Custom React Query hooks for all backend operations including user profiles, CT scans, scan analysis, and API configuration; updated `useGetExternalApiConfig` and `useConfigureExternalApi` to use the correct backend methods (`getExternalApiConfig` and `configureExternalApi` with `ExternalApiConfig` object), removing stale references to old separate endpoint/key methods.",
  "frontend/src/components/Header.tsx": "Application header with MediScan AI branding, user profile display, and an API Config button that is shown only when `isFetched` (from the updated `useIsCallerAdmin`) is true AND `isAdmin` is true, ensuring the button reliably appears after the admin check fully resolves.",
  "frontend/src/components/Footer.tsx": "Footer component with dynamic copyright year and medical disclaimer text only, with no external attribution links.",
  "frontend/src/components/LoginScreen.tsx": "Login screen with feature highlights and Internet Identity authentication.",
  "frontend/src/components/ProfileSetup.tsx": "Profile setup form for new users to enter their name, department, and specialization.",
  "frontend/src/components/ApiConfigDialog.tsx": "Admin-only API Configuration dialog rebuilt to match the exact spec — semi-transparent black overlay, centered white dialog ~500px wide, bold \"API Configuration\" title, labeled text input for \"API Endpoint URL\" and password-masked input for \"API Key\", gray-outline Cancel and solid-blue Save buttons, per-field inline validation errors, loading/disabled state on Save, and pre-filled values from the backend using the updated `useGetExternalApiConfig` and `useConfigureExternalApi` hooks.",
  "frontend/src/pages/Dashboard.tsx": "Main dashboard page with tabs for viewing all scans and uploading new scans.",
  "frontend/src/components/UploadScanTab.tsx": "Upload interface for CT scans with patient ID input, file selection, and image preview.",
  "frontend/src/components/ScansListTab.tsx": "Grid view of all uploaded CT scans with status indicators and click-to-view details.",
  "frontend/src/components/ScanCard.tsx": "Individual scan card component displaying thumbnail, patient ID, and analysis status.",
  "frontend/src/components/ScanDetailDialog.tsx": "Detailed view dialog showing original scan, analysis results, tumor mask overlay, and staging information; updated to import `useGetExternalApiConfig` instead of the removed `useGetApiConfig`, and updated property access from `apiConfig?.endpoint` to `apiConfig?.endpointUrl` to match the `ExternalApiConfig` type.",
  "frontend/src/index.css": "Global styles with OKLCH color system including custom medical theme colors for primary, secondary, accent, success, and warning states.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with medical theme color tokens for consistent styling across the application."
}